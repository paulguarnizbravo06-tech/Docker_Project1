version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: antigravity-docker-dashboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - DOCKERIZED=true
      - PORT=3000
      - NODE_ENV=production
